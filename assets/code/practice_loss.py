from numpy import ndarray


def practice_loss(x: ndarray, y: ndarray) -> float:
    """
    Calculate the 'distance' between `x` and `y`.
    You may see this loss function more in practice from different packages.
    It's not because this loss function is better, but because that it is more flexible.
    In this book we will still use `formal_loss` for the sake of explaining gradients.
    """
    return 0
