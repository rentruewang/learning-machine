from numpy import ndarray


def ml_model(x: ndarray) -> ndarray:
    """
    This is actually a machine learning model,
    well, they behave like functions,
    we only care about the relation between the input and the output,
    so we don't care about how it's implemented.
    Machine learning models are so popular only because they are smart functions.
    """
    return x
